# Work Instruction Superset Reconciliation — Implementation Plan

> **Approval reference:** 2026-08-27 user request to compare v4.8 r5.4, v4.9 Terra-Max and project-derived additions; preserve core capability, correct omissions, require startup core-fun/system/SWOT/remaining-work/work-order canon correction, absorb only project-neutral lessons, and route Visual binaries to exact project-local durable assets instead of mandatory Notion duplication.

## Goal

Extend the current Base Work bundle without copying historical instructions into a second canon.

The implementation preserves the merged startup checklist from PR #735 and the project-local Visual delivery owner from PR #736, then adds a thin discoverable evidence-identity contract for recurring Work→Codex→Godot→CI→GitHub/Notion closeout failures.

## Current authority

- original RED baseline: `1df8878d8a99d91a318ad4adff722d78c763a69a`
- project-local Visual main: `b0335f834b4a5d82f0e5978eb8ca88ab25fc47f4`
- proposal merged main: `b9c10d33e9096a5e213c081ddcfca7db9db4a58d`
- proposal: BCP-2026-039 / PR #737 / merged
- implementation: PR #738
- current implementation reconciliation commit: `67db7332a4cb1c2ac9264e0734048cefe3eb6825`
- current owners retained:
  - `templates/project-operations/WORK_CODEX_MINIMUM_TRANSITION_STARTER_PROMPT.md`
  - `templates/project-operations/WORK_PROJECT_START_CANON_CHECKLIST.md`
  - `templates/project-operations/WORK_CODEX_MINIMUM_TRANSITION_VERTICAL_SLICE_PROFILE.md`
  - `templates/project-operations/WORK_PROJECT_LOCAL_VISUAL_ASSET_DELIVERY_PROFILE.md`
  - current CI, Godot, Visual, Git synchronization and continuous-work owners

## Non-regression boundary

The implementation must preserve:

- core fun, player promise, core systems, evidence-based SWOT, current stage, remaining work and work order startup Gate;
- Work preparation → one Codex implementation window → Work final review → user validation;
- Reuse First, market/success/failure comparison, minimum three real alternatives;
- Brainstorming/Superpowers, TDD, systematic debugging and verification-before-completion when triggered;
- actual-consumer Visual production, provenance/rights and runtime promotion evidence;
- safe Git fetch/FF-only pull/current-task push/PR/merge/readback;
- GUT/Hera or adopted-equivalent machine QA without Human/Player overclaim;
- fallback ladder, incident→solution→lesson, required-work zero rescan and minimum five full adversarial loops.

Project-specific PR numbers, SHAs, paths, resolutions, styles, characters and current priorities must not leak into active generic policy except where this Case records its own Base lifecycle history.

## Approved implementation scope

1. Add a project-neutral evidence/identity owner.
2. Add a thin current router that loads existing owners instead of duplicating them.
3. Add a non-regression/case receipt explaining what was preserved, improved, delegated, rejected as project-specific or corrected as unsafe generalization.
4. Update the focused contract from RED to current-policy GREEN.
5. Verify exact-head required workflows, minimum five full-scope adversarial loops, safe squash merge and new-main readback.

## Excluded and protected scope

- no modification or takeover of another open PR;
- no `[수정제안서]/PROPOSAL_REGISTRY.json` update while PR #678 owns it;
- no engine baseline, dependency, provider, CI workflow or ruleset change;
- no project-specific name, PR/Issue, SHA, path, resolution, UI composition, style lock or completed feature list generalized into Base;
- no Human/Player evidence promotion;
- no direct main push, force push, reset/clean or ruleset bypass;
- no restoration of mandatory Notion image binary upload under the explicit project-local Visual profile.

## TDD task sequence

### Task 1 — RED contract

Created `tests/test_work_instruction_superset_reconciliation_contract.py` first.

RED exact head:

```text
b70d3189918fdf01b25d1a1bfb9e588820f29179
```

Observed workflows:

```text
Validate Base v9 Operating Contracts: PASS
Validate Game Project Operating System: expected FAIL
```

The focused/core regression failed because the required owner/router/case did not exist; unrelated Base contract validation remained green.

### Task 2 — minimal GREEN owner

Added only:

- `templates/project-operations/WORK_EXECUTION_EVIDENCE_IDENTITY_INTEGRITY.md`
- `templates/project-operations/WORK_PROJECT_EXECUTION_CURRENT_ROUTER.md`
- `docs/knowledge/cases/WORK_INSTRUCTION_SUPERSET_RECONCILIATION_CASE.md`

Updated the focused test so Visual details are delegated to the merged PR #736 owner:

```text
WORK_PROJECT_LOCAL_VISUAL_ASSET_DELIVERY_PROFILE.md
LOCAL_VISUAL_CANDIDATE
!= PROJECT_ASSET_APPROVED
!= RUNTIME_PROMOTED
NO_NOTION_BINARY_UPLOAD_REQUIRED
```

The first GREEN attempt found one valid regression: the router was 218 lines and violated its thin-router contract. It was reduced to 158 lines without removing routed capability.

### Task 3 — regression and adversarial verification

Run/observe on exact current head:

- Python syntax;
- focused unittest;
- current Base required workflows;
- changed-file and open-PR concurrency audit;
- review/thread/conflict state;
- minimum five full-scope adversarial loops.

Each full loop rechecks the whole approved scope:

1. user intent / authority / v4.8-v4.9 non-regression;
2. startup checklist / core fun / Work↔Codex minimum transition;
3. project-local Visual / Notion semantics / durability / rights;
4. candidate freshness / CI result chain / Godot source identity / remote sync;
5. project-specific leakage / Human-Player evidence / public-cost-security boundary.

A valid finding reopens correction and resulting-state verification.

### Task 4 — closeout

- update PR #738 body with exact RED/GREEN and exact-head evidence;
- verify unresolved threads, reviews and conflicts;
- mark ready only after current required checks pass;
- squash merge only with exact expected head;
- read back new `main` and new files;
- recalculate remaining machine-executable work;
- generate the final single copy-paste Work instruction artifact for the user.

## Rollback

Revert the implementation squash commit and remove the evidence owner link from the current router, then rerun focused/core regression.

Existing v4.9 Starter, PR #735 startup checklist, PR #736 local Visual owner, PR #737 proposal record, project canons, product code, assets, engine baseline and CI workflow remain intact because this implementation is additive and thin-routed.
