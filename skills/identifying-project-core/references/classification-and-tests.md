# 프로젝트 코어 상세 판정표

## 코어 층

- 기획 코어: 핵심 행동·선택·위험·보상·감정·판타지·세계관·시각·톤의 불변 방향과 금지 방향.
- 시스템 코어: 입력과 자원 → 핵심 행동 → 상태·가치 변화 → 활용·보상 → 다음 행동의 중심 순환.
- 기술 기반: 게임·세션 상태, 저장·호환성, 엔티티 생성, 전투·경제·규칙 판정, 이벤트·메시지, 공통 ID와 Schema. 기술 중심성이 곧 제품 정체성은 아니다.

## 증거 우선순위

최신 사용자 승인 → 등록된 책임 원본 → 결정 기록·현재 상태 → 실제 코드·데이터·자산·테스트 → 과거 문서·외부 설명.

현재 구현만 있고 승인 근거가 없거나 문서만 있고 구현·검증이 없으면 같은 상태로 합치지 않는다.

## 후보 기록

```yaml
candidate:
layer: design/system/technical
claimed_role:
evidence:
dependents:
replaceable_implementation:
current_status:
```

## 의존 관계 확인

- 어떤 시스템·콘텐츠·UI·데이터가 후보에 의존하는가?
- 후보가 다른 코어를 지탱하는가, 편의만 제공하는가?
- 대체 구현으로 같은 경험과 사용자 약속을 유지할 수 있는가?
- 코드 의존성과 제품 정체성 의존성이 같은가?

## 제거·대체·변경 테스트

1. 제거하면 핵심 행동과 플레이어 약속이 성립하는가?
2. 다른 방식으로 대체해도 같은 경험인가?
3. 축소하면 뾰족한 재미와 반복 동기가 유지되는가?
4. 변경하면 장르·판타지·사용자 약속이 달라지는가?
5. 다른 주요 시스템이 끊기거나 의미를 잃는가?
6. UI·대사·스킨·연출·콘텐츠 수량처럼 교체 가능한 외피인가?
7. 구현 방식만 바뀌고 제품 의미는 유지되는가?

## Core와 MVP

- `CORE`: 정체성과 핵심 경험에 필수.
- `MVP_SUPPORT`: 정체성은 아니지만 핵심 가설을 실행·관찰하는 데 필수.
- `BOTH`: 코어이며 현재 MVP에도 필요.
- `LATER`: 코어도 아니고 현재 검증에도 불필요.

## 판정 상태와 실패 경계

- `IDENTIFIED`: 근거·경계·의존 관계가 명확하다.
- `PARTIAL`: 일부 층만 확인됐다.
- `CONFLICTED`: 승인 원본·문서·실제 구현이 충돌한다.
- `UNVERIFIED`: 필요한 근거가 없다.

높은 제작비, 현재 구현 여부, 장르명, 홍보 문구, 기능 목록만으로 코어를 확정하지 않는다. 모든 중요 요소를 `PROJECT_CORE`로 만들거나 기술 기반과 플레이어 경험을 혼동하거나 읽기 전용 판정 중 책임 원본을 수정하면 실패다.
