name: Temporary project core skill synchronization

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]' && github.event.pull_request.head.ref == 'agent/project-core-and-adversarial-skills-v2' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply synchronized patch
        run: python tools/apply_project_core_skills.py

      - name: Install validation dependency
        run: python -m pip install jsonschema

      - name: Run focused regression tests
        run: |
          python -m unittest \
            tests.test_game_project_operating_system_structure \
            tests.test_skill_package_integrity \
            tests.test_consolidated_skill_references \
            tests.test_reference_freshness
          git diff --check

      - name: Commit synchronized changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Register project core and adversarial review skills"

      - name: Validate canonical reference freshness
        run: |
          python tools/check_canonical_reference_freshness.py \
            --config .github/reference-freshness.json \
            --base origin/main \
            --head HEAD

      - name: Push synchronized branch
        run: git push origin HEAD:${{ github.event.pull_request.head.ref }}
